name: multi-platform-build

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Prepare config.ini (from example)
        shell: bash
        run: |
          cp -f config.example.ini config.ini

      - name: Build (PyInstaller)
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            pyinstaller perf_test.spec --clean
          else
            pyinstaller perf_test.macos.spec --clean
          fi

      - name: Add runtime folders/files
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "macOS" ]]; then
            TARGET_ROOT="dist/perf_test.app/Contents/MacOS"
          else
            TARGET_ROOT="dist/perf_test"
          fi

          mkdir -p "$TARGET_ROOT/Answer" "$TARGET_ROOT/DB" "$TARGET_ROOT/Log" \
                   "$TARGET_ROOT/Upload_folder/ARC" "$TARGET_ROOT/Upload_folder/Health" "$TARGET_ROOT/Upload_folder/Employment"
          # Include DB CSVs required by the app
          cp -f DB/*.csv "$TARGET_ROOT/DB/" || true
          # Ensure excel templates are always present in artifact
          mkdir -p "$TARGET_ROOT/excel_template"
          cp -Rf excel_template/. "$TARGET_ROOT/excel_template/" || true

      - name: Archive artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          if (Test-Path dist\\perf_test.zip) { Remove-Item dist\\perf_test.zip -Force }
          Compress-Archive -Path dist\\perf_test -DestinationPath dist\\perf_test-${{ runner.os }}.zip

      - name: Archive artifact (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          cd dist
          zip -r "perf_test-${{ runner.os }}.zip" perf_test.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: perf_test-${{ runner.os }}
          path: dist/perf_test-${{ runner.os }}.zip
